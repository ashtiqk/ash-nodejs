version: 0.2

phases:
  install:
    commands:
      - echo nothing to install...
  pre_build:
    commands:
      - echo no pre build
  build:
    commands:
      - echo no build
  post_build:
    commands:
artifacts:
  files:
    - **/*
